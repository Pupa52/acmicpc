#include <iostream>
#include <algorithm>
#include <vector>

using namespace std;

typedef long long ll;

void solution()
{
	int a, b;
	cin >> a >> b;

	cout << a * b << endl;
}

int main(void)
{
	ios::sync_with_stdio(false);
	cin.tie(NULL);

	solution();

	return 0;
}
