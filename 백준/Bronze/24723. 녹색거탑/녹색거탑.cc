#include <iostream>
#include <vector>
#include <cmath>

using namespace std;

void solution()
{
	int n;
	cin >> n;

	cout << pow(2, n);
}

int main(void)
{
	ios::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	solution();

	return 0;
}
