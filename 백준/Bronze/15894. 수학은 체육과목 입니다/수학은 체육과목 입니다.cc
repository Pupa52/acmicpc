#include <iostream>
#include <vector>

using namespace std;

using pii = pair<int, int>;

void solution()
{
	unsigned int n;
	cin >> n;

	cout << n * 4 << endl;
}

int main(void)
{
	ios::sync_with_stdio(false);
	cin.tie(NULL);

	solution();

	return 0;
}
